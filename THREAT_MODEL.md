# msquill threat model

## 1. Overview

msquill is a MathQuill-derived JavaScript library for editable math, static math and text fields. An application creates a field around a DOM element; the library maintains an expression tree, cursor and selection, parses LaTeX-like input, renders HTML through jQuery, and exports text, LaTeX or HTML. It runs inside the embedding page’s origin and JavaScript authority. It is not an authentication service or a storage backend. The public API constructs fields from elements and options (`src/publicapi.js:37`); the package entry point is `build/mathquill.js` (`package.json:4`).

The normal runtime is an application bundle containing the checked-in or rebuilt CommonJS artifact and a host-provided jQuery resolution. A separate developer server watches source and rebuilds tests. That server has filesystem and process authority which the browser field does not possess. Build and developer tools must therefore remain distinct from the embedded component.

| Component | Input and purpose | Source |
| --- | --- | --- |
| Public field API | DOM element, options, LaTeX and edit commands | `src/publicapi.js:37` |
| Parser/controller | Expression text → command tree → field update | `src/services/latex.js:86`, `src/services/latex.js:107` |
| Text/tree rendering | Command HTML and text nodes → jQuery DOM insertion | `src/commands/text.js:68`, `src/tree.js:105` |
| Configurable grammar | Per-controller dictionaries and caller-supplied symbol definitions | `src/services/commands.js:3`, `src/services/commands.js:94` |
| Developer HTTP/build tool | Request paths, filesystem watches, local test execution | `script/test_server.js:13`, `script/test_server.js:78` |
| React webpack development server | Nested package start → webpack assets/public files and development middleware | react-testing-environment/package.json:57-60; react-testing-environment/scripts/start.js:43-94; react-testing-environment/config/webpackDevServer.config.js:31-101 |

| Workflow | Configuration chain | Effective resource | Recipients | Control or obligation | Evidence |
| --- | --- | --- | --- | --- | --- |
| Library loading | Package main → distributed artifact | `build/mathquill.js`, requiring `jquery` | Host bundle/browser | Trusted dependency and artifact provenance | `package.json:4`, `build/mathquill.js:1` |
| Expression rendering | `latex`/write → parser → command HTML | Caller-owned `.mq-root-block` DOM | Scripts/users in host origin | Full-input syntax checks; HTML safety belongs to rendering implementations | `src/services/latex.js:92`, `src/services/latex.js:126` |
| Symbol definitions | Host options → grammar processor → constructor | `htmlEntity` is rendering content, per-controller grammar dictionaries are in memory | Browser DOM in the host origin | Trusted application configuration required | `src/services/commands.js:3`, `src/services/commands.js:94` |
| Development serving | HOST/PORT defaults; normalize URL pathname then remove first character | 0.0.0.0:9292; ordinary paths resolve from process cwd, but on Windows /C:/... can become drive-absolute C:\... and read outside cwd | Reachable HTTP clients | OS file permissions only; normalization does not establish document-root containment. Bind narrowly and resolve/reject paths against an explicit root | script/test_server.js:9-25 |
| Development builds | Startup and watched-file changes → shell command | `make test`, inherited working directory/environment | Developer account and tools | Trusted local source/build authority | `script/test_server.js:15`, `script/test_server.js:78` |
| React debugging (separate server) | Nested package start → scripts/start.js; HOST default 0.0.0.0, PORT default 3000 with choosePort fallback | Public files under process-cwd/public (normally react-testing-environment/public), in-memory webpack assets and source-map/error-overlay/hot-reload middleware | Reachable HTTP/browser clients; development process/tools | disableHostCheck is true without proxy or with DANGEROUSLY_DISABLE_HOST_CHECK=true; static contentBase does not describe all middleware. Use loopback, enable host checks, and assess any configured proxy/middleware separately | README.md:31; react-testing-environment/scripts/start.js:43-94; react-testing-environment/config/paths.js:9-10,73; react-testing-environment/config/webpackDevServer.config.js:31-101 |

The Makefile describes full/basic/minified outputs and distribution paths. The important renderer and text-serialization behavior is also present in the package’s main artifact, so it is not merely an unused source surface (`Makefile:48`, `build/mathquill.js:2166`, `build/mathquill.js:3175`). This review does not establish byte-for-byte build reproducibility.

## 2. Threat Model, Trust Boundaries, and Assumptions

The protected assets are host-origin DOM integrity, expression fidelity, editor availability, and the developer workspace when its server is started. Confidentiality depends on the embedding page’s access controls: node IDs and field lookup APIs are object references, not user or tenant authorization (`src/publicapi.js:15`). A hostile expression becomes a cross-user boundary only when a host imports or displays one user’s content to another. No particular host deployment is assumed.

Expression text can enter through programmatic setters, writes, original element text or paste. Parsing rejects absent command lookups and requires end-of-input before accepting a complete math write, but command dictionaries are ordinary objects. An inherited name such as \constructor resolves to Object rather than the unknown-command branch, and the subsequent .parser(cursor) call raises TypeError, aborting the operation (src/tree.js:370; src/services/commands.js:5-7; src/services/latex.js:43-49). Those checks establish syntactic recognition, not HTML encoding (`src/services/latex.js:43`, `src/services/latex.js:92`). TextPiece uses `document.createTextNode`, while TextBlock’s HTML representation concatenates `textContents()` into a span. Both facts matter: safety in an interactive insertion path does not establish safety when the same content is serialized and rendered again (`src/commands/text.js:68`, `src/commands/text.js:198`). This is a source-established HTML injection path: the text parser accepts angle-bracket markup within a text command, TextBlock.html concatenates it unescaped, and renderLatexMath inserts the joined HTML via jQuery .html(). Initial element text and .latex() reach this path; public .write() and normal paste also send parsed TextBlock HTML through block.jQize() and insertBefore() (src/publicapi.js:145-148; src/services/textarea.js:114; src/services/latex.js:86-97; src/tree.js:104-106) (src/publicapi.js:60-63; src/publicapi.js:80-84; src/commands/text.js:48-72; src/services/latex.js:113-126). Cross-user reach and resulting script authority depend on host inputs and browser policy. StaticMath initialization has an independent selectable-copy sink: after initRoot parses original element text, staticMathTextareaEvents concatenates exportLatex() into an HTML string passed to jQuery prepend. Ordinary math characters can serialize back into angle-bracket markup here without a text command or whole-string VanillaSymbol input. Construct the selectable span separately and set its contents as text; correcting the two expression-renderer sinks does not protect this copy. The same prepend exists in the distributed artifact (src/publicapi.js:60-63; src/publicapi.js:105-107; src/services/latex.js:27-28; src/services/latex.js:83-84; src/services/textarea.js:48-52; build/mathquill.js:2451-2455).

This fork adds per-controller grammar configuration. `options.commands` is transformed into command constructors, and `symbolDefinition.htmlEntity` is passed directly to VanillaSymbol, interpolated into its HTML template and parsed by normal jQize insertion. This configuration-to-DOM mechanism is source-established; attacker-controlled configuration and resulting cross-user/script impact remain conditional. Copying the grammar dictionary separates editor configuration, but does not make supplied HTML untrusted data. The host must own command definitions and callbacks (`src/services/commands.js:19`, `src/services/commands.js:69`, `src/services/commands.js:94-99`; `src/commands/math.js:335-339`; `src/tree.js:104-129`).

Clipboard settings change interpretation. `statelessClipboard` removes surrounding dollar delimiters or wraps pasted text in a text command. It is a format convention, not an escaping policy (`src/services/textarea.js:95`). `overrideLatexPaste` passes the entire clipboard string to typedText in one call. In a math block, an unmatched multi-character string falls through to VanillaSymbol, which interpolates it unescaped into a span before jQize/createLeftOf parses and inserts the HTML. Non-command public .cmd() strings reach the same sink. This is a second source-established HTML injection path independent of TextBlock; preserve input as text in VanillaSymbol rendering, or use character-wise insertion with verified text semantics (src/services/textarea.js:89-114; src/commands/math.js:335-339; src/commands/math.js:477-497; src/publicapi.js:151-164; src/tree.js:104-129). Output returned by an HTML export also remains subject to the destination context’s rules; a host cannot treat an expression parser as a general HTML sanitizer.

A realistic attacker may control expression or clipboard content where the host permits it, or HTTP requests to an explicitly started reachable developer server. They are not assumed to control application scripts, trusted options, installed dependencies, build source or the developer account. The developer request handler reads files; the separate rebuild trigger uses local filesystem changes. This does not establish that a remote request supplies shell command text (`script/test_server.js:24`, `script/test_server.js:78`). React debugging additionally starts an independent webpack server: default 0.0.0.0:3000 (or a chosen free port), host checking disabled with the checked-in no-proxy configuration, public/build assets plus registered source-map and error-overlay middleware. Dependencies own the latter implementations; remote execution is not established (react-testing-environment/scripts/start.js:43-94; react-testing-environment/config/webpackDevServer.config.js:31-101).

Host applications must enforce persistence permissions, cross-user sharing, output context, and input size/depth limits. The inspected parser/renderer runs in-process without an established workload-isolation boundary. Package metadata declares `private: true`; jQuery is required by the artifact but absent from declared dependencies, so actual resolution is an integration concern (`package.json:5`, `src/intro.js:1`). No external deployment configuration or current dependency compatibility was verified.

## 3. Attack Surface, Mitigations, and Attacker Stories

TextBlock, direct VanillaSymbol, configurable htmlEntity and StaticMath selectable-copy HTML insertion paths, plus the prototype-name command exception, are established from source. Other rows are investigation hypotheses. Actual attacker control, cross-user impact and script authority require host exposure and controls. Ordinary authorized editing or application-owned rendering HTML is not an attack.

| Priority | Scenario and capability gain | Prerequisites | Impact | Existing controls | Mitigation | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| High, conditional impact | Text-command content is concatenated unescaped and inserted as HTML through initial content, .latex(), .write() or normal paste; injection at this sink is source-established | Host supplies attacker-controlled text-command content; cross-user impact requires rendering it in a victim context | Host-origin DOM alteration; script authority depends on executable markup and browser/host policy | Parser recognition is not encoding; TextPiece text nodes in other paths do not protect TextBlock.html plus jQuery .html()/jQize insertion | Preserve text-as-data in TextBlock rendering and verify setters, write, paste and round-trip paths; do not accept untrusted expressions until this sink is made safe | src/publicapi.js:60-63; src/publicapi.js:80-84; src/commands/text.js:48-72; src/services/latex.js:86-126; src/publicapi.js:145-148; src/services/textarea.js:114; src/tree.js:104-106 |
| High, conditional impact | Whole-string override paste or non-command .cmd() input becomes VanillaSymbol HTML; source-established independently of TextBlock | Host enables overrideLatexPaste and accepts controlled clipboard markup into a math block, or supplies controlled non-command .cmd() strings | Host-origin DOM alteration; cross-user/script impact depends on host exposure and browser policy | LaTeX parsing is bypassed; VanillaSymbol interpolates raw fallback input | Preserve text-as-data in VanillaSymbol or validate character-wise insertion semantics; fixing TextBlock alone does not repair this route | src/services/textarea.js:89-114; src/commands/math.js:335-339; src/commands/math.js:477-497; src/publicapi.js:151-164; src/tree.js:104-129 |
| High, conditional impact | Configured htmlEntity is interpolated into VanillaSymbol HTML and inserted by jQuery; the mechanism is source-established | Host accepts attacker-controlled commands definitions and renders the resulting symbol | Host-origin DOM alteration; cross-user/script compromise depends on victim use and browser/host policy | Dictionary copies separate configuration but do not sanitize symbol HTML | Keep command definitions application-owned; if declarative configuration is less trusted, constrain it to safe text/approved symbols before rendering rather than accepting arbitrary HTML | src/services/commands.js:94-99; src/commands/math.js:335-339; src/tree.js:104-129 |
| High, conditional impact | StaticMath initial content is exported back into the selectable-copy HTML passed to prepend; independent source-established sink | Host initializes StaticMath from attacker-controlled element text that parses and serializes into markup; no text command or whole-string fallback required | Host-origin DOM alteration; executable-markup and victim authority depend on browser/host policy | Original text extraction and successful math parsing do not HTML-encode exportLatex output | Create the selectable span as an element and set the dollar-delimited export as text; verify independently of TextBlock/VanillaSymbol corrections | src/publicapi.js:60-63; src/publicapi.js:105-107; src/services/latex.js:83-84; src/services/textarea.js:48-52; build/mathquill.js:2451-2455 |
| Medium, conditional | Deep or very large input monopolizes parsing/rendering | Host accepts unbounded supplied expressions | Editor freeze; broader UI unavailability in shared page | Parser failure handles absent names, but inherited prototype names can throw; no general workload budget is established | Host length/depth limits; isolate expensive processing if needed | `src/services/latex.js:62`, `src/services/latex.js:107` |
| Low locally; higher only with material shared impact | Small prototype-name input such as \constructor bypasses the missing-command branch and raises TypeError | Accepted LaTeX reaches an ordinary-object command dictionary; another user’s impact requires a host sharing/rendering boundary | Aborted setter/write operation; broader availability loss depends on host error handling | End-of-input and parser alternatives do not convert this JavaScript exception into a normal parser failure | Use own-property command lookup or null-prototype dictionaries consistently, including copied dictionaries, and safely reject non-command entries | src/tree.js:370; src/services/commands.js:5-7; src/services/latex.js:43-49 |
| Medium to High, conditional | Reachable development file server exposes workspace files or, on Windows, process-readable files outside the workspace | Explicit startup and network reachability; Windows drive-absolute request paths can survive normalize(...).slice(1); sensitive readable target exists | Source or confidential host-file disclosure | OS read permissions; no explicit document root or containment check | Restrict binding and enforce a document-root containment check after platform-aware absolute resolution; reject escapes | script/test_server.js:9-25 |
| Medium, conditional | Reachable React webpack server exposes development assets/source information or middleware capabilities to unintended clients | Separate nested server is started; network/browser path reaches it; default no-proxy configuration disables host checking | Development content disclosure; further middleware impact requires dependency-specific validation | contentBase limits static files to public but generated assets and source-map/error-overlay middleware are separate surfaces | Bind loopback, enable host validation and scope any proxy/middleware; assess the actual dependency implementation before claiming further impact | react-testing-environment/scripts/start.js:43-94; react-testing-environment/config/webpackDevServer.config.js:31-101 |
| Context-dependent | Altered dependency/build input becomes shipped JavaScript | Attacker gains a lower-trust build-input channel | Consumers execute modified library code | Explicit build inputs and package main identify artifact path | Control dependency resolution and verify distributed artifacts | `package.json:4`, `Makefile:48` |

Validation should exercise actual host-supported input/render routes, including StaticMath initial-content selectable copies, configured htmlEntity, text serialization and paste options, before assigning exploitability. The TextBlock, direct/configured VanillaSymbol and StaticMath selectable-copy paths already establish HTML insertion of unescaped data; resulting script execution and victim authority still require host-specific assessment, and a malformed expression affecting only its author is not automatically a cross-user compromise. Developer exposure must be measured against the actual binding, workspace and network, rather than inferred from the existence of a script.

## 4. Severity Calibration (Critical, High, Medium, Low)

**Critical:** Requires extraordinary demonstrated reach, such as distribution compromise delivering attacker JavaScript across many privileged consuming applications. A malicious expression alone does not establish that reach. Repository or maintainer control already possessed by an attacker is not itself a new privilege gain.

**High:** A demonstrated cross-user expression-to-script boundary that executes with a victim’s meaningful application authority can qualify. Exposure of materially sensitive developer files may also qualify when the listener is actually reachable. Trusted application-supplied HTML and a developer intentionally opening their own file are counterexamples.

**Medium:** Reproducible hostile-input freezes affecting other users’ application use, or limited unintended disclosure from a reachable development workspace, can qualify. Large local expressions with only self-inflicted slowdown require lower calibration; absence of a generic size limit alone is not enough.

**Low:** Localized rendering corruption, recoverable editor errors, or narrowly scoped formatting defects without meaningful authority or availability loss. Pure semantic differences are not security findings unless a host relies on them for a security-relevant decision.

This is a source-backed architecture model, not completed audit coverage. It leaves actual host exposure, data sharing, dependency resolution and operational controls explicit rather than inventing them.

---

Repository: https://github.com/mathspace/msquill  
Version: `f393b3c26bb31ceed534d7980748b3bf9321aea3`
