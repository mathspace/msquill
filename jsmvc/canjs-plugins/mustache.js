/*globals
 define:false
 */
define([
    'can/view/mustache'
], function(can) {
    'use strict';
    // WARNING: Unlike `can/view/stache`, `can/view/mustache` returns the `can`
    //          namespace!!!
    return can;
});
